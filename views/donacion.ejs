<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donación - ECO Culiacán</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="shortcut icon" href="/images/ECO.png">
    <style>
    </style>
</head>
<body>
    <header class="encabezado">
        <div class="logo">
            <a href="/"><img src="/images/ECO.png" alt="Logo ECO Culiacán"><span></span></a>
        </div>
        <div>
            <a href="/bienvenida"><button class="secondary-btn">No deseo donar</button></a>
        </div>
    </header>

    <main class="main-content">
        <% if (success_msg && success_msg.length > 0) { %>
            <div class="flash-message success">
                <%= success_msg %>
            </div>
        <% } %>
        <% if (error_msg && error_msg.length > 0) { %>
            <div class="flash-message error">
                <%- error_msg %>
            </div>
        <% } %>
        <% if (error && error.length > 0) { %>
            <div class="flash-message error">
                <%= error %>
            </div>
        <% } %>
        <div class="donation-container">
            <h1>¡Tu aporte es importante para nosotros!</h1>
            <form action="/donacion" method="POST">
                <div class="form-group">
                    <label for="cardNumber"><img src="/images/iconotarjeta.png" alt="Tarjeta" width="28px"> Número de Tarjeta</label>
                    <input type="text" id="cardNumber" name="cardNumber" value="<%= formData.cardNumber || '' %>" placeholder="Tarjeta" oninput="onlyNumber(this, 16)" required>
                </div>
                <div class="form-group">
                    <label for="cardHolder"><img src="/images/iconousuario.png" alt="Titular" width="28px"> Nombre del Titular</label>
                    <input type="text" id="cardHolder" name="cardHolder" value="<%= formData.cardHolder || '' %>"placeholder="Nombre" oninput="inputName(this)" required>
                </div>
                <div class="form-row">
                    <div class="form-group">
                        <label for="expiryDate"><img src="/images/iconofecha.png" alt="Vencimiento" width="23px"> Fecha de Vencimiento</label>
                        <input type="text" id="expiryDate" name="expiryDate" value="<%= formData.expiryDate || '' %>"placeholder="MM/AA" pattern="(0[1-9]|1[0-2])\/\d{2}" oninput="inputName(this)" required>
                    </div>
                    <div class="form-group">
                        <label for="cvv"><img src="/images/iconocandado.png" alt="CVV" width="23px"> CVV</label>
                        <input type="text" id="cvv" name="cvv" value="<%= formData.cvv || '' %>" placeholder="CVV" oninput="onlyNumber(this, 3)" required>
                    </div>
                </div>
                <button type="submit" class="btn-donate">Donar</button>
            </form>
        </div>
    </main>

    <footer class="pie">
        <p>Menos basura, más futuro</p>
        <a href="https://ECOCuliacan.com.mx/" target="_blank">ECOCuliacán.com.mx</a>
        <div class="social-icons">
            <a href="https://www.facebook.com/share/16Je91rWsc" target="_blank"><img src="/images/facebook.png" alt="Facebook" class="social-icon"></a>
            <a href="https://x.com/ECOCuliacan" target="_blank"><img src="/images/twitter.png" alt="Twitter" class="social-icon"></a>
            <a href="https://www.instagram.com/eco.culiacan/profilecard/" target="_blank"><img src="/images/ig.png" alt="Instagram" class="social-icon"></a>
        </div>
    </footer>

    <script src="/events.js"></script>
</body>
</html>